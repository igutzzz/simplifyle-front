import React from "react";

const Subtitle: React.FC = () => {
  return (
    <div>
        <h3 className="text-xl font-light tracking-tighter lg:text-1xl">a better (and fast) way to share files.</h3>
    </div>
  );
};

export default Subtitle;